service:
  type: ClusterIP

ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: traefik
  hosts:
    - host: grafana.localhost
      paths:
        - path: /
          pathType: Prefix