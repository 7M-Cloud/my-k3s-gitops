service:
  type: ClusterIP

ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: traefik
  hosts:
    - grafana.localhost
  path: /
  pathType: Prefix

# 👇 ADD THIS BLOCK TO FIX THE ERROR
rbac:
  pspEnabled: false