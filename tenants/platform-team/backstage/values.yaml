backstage:
  appConfig:
    app:
      baseUrl: http://backstage.localhost
    backend:
      baseUrl: http://backstage.localhost
      listen:
        port: 7007
ingress:
  enabled: true
  className: traefik
  host: backstage.localhost
